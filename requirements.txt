# Exact versions of the environment the reported runs used, as stated in the manuscripts.
# Python 3.10.12.
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.7.2      # four-decimal logistic-regression AUROCs move across minor versions
h5py==3.16.0
torch==2.10.0            # CUDA 12.8 build for the graph experiments; CPU is enough for the checks
torch-geometric==2.7.0   # graph experiments only
# Used by the analysis and figure scripts. No reported number depends on their versions, and the
# run environment did not record them, so these are lower bounds and not pins.
pandas>=2.0              # naive_baselines_allfolds.py
joblib>=1.3              # naive_baselines_allfolds.py
matplotlib>=3.7          # make_figures_p1.py, make_figures.py
