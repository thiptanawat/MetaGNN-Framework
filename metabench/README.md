# metabench

Five checks for benchmarks whose labels are shared across samples. Framework
agnostic: each function takes numpy arrays that any pipeline already produces.

See `../docs/PROTOCOL.md` for the recipe and `../README.md` for what these checks
found on a real benchmark.
