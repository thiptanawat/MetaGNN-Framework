# Minimum requirements to run the test-suite locally / in CI.
# Heavy / optional packages (torch_geometric, cobra, rdkit) are detected at
# import-time and the relevant tests are auto-skipped if they are missing,
# so they do NOT need to be installed for the suite to pass.

# ── Core test runner ────────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0

# ── Pure-function unit tests stack (always required) ────────────────────
numpy>=1.21
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0
matplotlib>=3.4

# ── Optional, only loaded if available ─────────────────────────────────
# torch>=2.0
# torch-geometric>=2.4
# cobra>=0.29
# rdkit                # via conda: conda install -c conda-forge rdkit
# h5py>=3.0
